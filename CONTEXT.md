# Контекст проекту Multimarket

## 2026-08-17 14:30
Синхронізовано робочу папку (Multimarket/multimarket-site) з GitHub — вона була суттєво застарілою: бракувало 4 товарів (bk19-22), кількох фотогалерей (h01, h07, h13, g11, p01, p07, tt06, tt07) і полів image для bk01-18. Тепер підключена як git-репозиторій (origin = github.com/cravecom64-cmyk/multimarket-site), синхронізована з main один-в-один. Далі working tree = джерело істини, можна редагувати напряму і push/pull звідси без обхідних шляхів через /tmp/repo.

Також цього ж дня: додано категорію "Блекаут" у Header/Footer/SEO-описи (раніше була тільки в динамічних списках), виправлено lifestyle-фото bk19 (декілька ітерацій — світло/промінь/ракурс/режим прожекторів), додано реквізити продавця в оферту + переписано /returns.

Далі: Павло просив звірити асортимент постачальника od.tanu.ua (топ-товари) і розширити кожну категорію сайту до 20-25 позицій.
